# 依赖与构建产物
node_modules
.next
npm-debug.log*
pnpm-debug.log*

# 环境变量（不进入镜像，运行期由 docker-compose 注入）
.env
.env.*
!.env.example

# Git 与本地文件
.git
.gitignore
NUL

# 部署文件
Dockerfile
docker-compose.yml
.dockerignore

# 日志与临时文件
*.log
.DS_Store
